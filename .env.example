# Backup Agent Configuration

# Panel URL (where backup-panel is hosted)
BACKUP_PANEL_URL=https://backups.example.com

# API Token (set automatically after verification)
BACKUP_API_TOKEN=

# Server name (defaults to hostname)
BACKUP_SERVER_NAME=

# Path to scan for Laravel sites (Forge default: /home/forge)
BACKUP_SITES_PATH=/home/forge

# Local storage path for temporary files
BACKUP_STORAGE_PATH=/tmp/backups

# Rsync destination (user@host:/path)
BACKUP_RSYNC_DESTINATION=backup@backup-server.example.com:/backups
