name: Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: zlib, phar
          ini-values: phar.readonly=0

      - name: Install dependencies
        run: composer install --prefer-dist --no-interaction

      - name: Install Box globally
        run: composer global require humbug/box

      - name: Build Phar
        run: |
          mkdir -p builds
          ~/.composer/vendor/bin/box compile
          mv backup-agent.phar builds/

      - name: Build standalone binary
        run: ./vendor/bin/phpacker build linux x64 --src=./builds/backup-agent.phar --php=8.4 --no-interaction

      - name: Prepare release files
        run: |
          mv builds/build/linux/linux-x64 builds/backup-agent-linux-x64
          chmod +x builds/backup-agent-linux-x64

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backup-agent-binaries
          path: |
            builds/backup-agent.phar
            builds/backup-agent-linux-x64

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            builds/backup-agent.phar
            builds/backup-agent-linux-x64
